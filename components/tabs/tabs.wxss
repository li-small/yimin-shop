.tabs_title {
  display: flex;
  padding: 15rpx 0;
}
.title_item {
  padding: 8rpx;
  text-align: center;
  flex: 1;
}

.active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}